//Write a program to convert the temperature from Celsius to Fahrenheit and vice verse.

//var convert:string="c";
//var result :number=(9/5)+32;
//console.log (result);

//Write a program that calculates the percentage.

//var num:number=18;
//var n2 :number=9;
//var result:number=18/9*100;
//console .log(result);

/*Write a program that calculates the discount for a product based on its price. If the price is 
above $100, apply a 10% discount; otherwise, apply a 5% discount.*/
/*var price=120;
if (price<=100) {
    var discount=5;
    var discount=5/100*price;
    var disountedPrice=price-discount;
    console.log("the disount on",price,"$ is",discount,"so the sales price is",disountedPrice)
}
else{
    var discount2=10;
    var discount2=10/100*price;
    var discountedprice2=price-discount2
    console.log("the discount on ",price,"$ is" ,discount2,"so the sale price is",discountedprice2)
}
*/
/*create a program that determines the category of a user-provided age. If the age is between 0 and 12, print 
"Child."If it's between 13 and 19, print "Teenager." Otherwise, print "Adult."*/
/*var age=15;
if(age>=0 && age<=12){
    console.log("child")
}
var age=20;
if(age>=12 && age<=20){
    console.log("teenager")
}
else{
    console.log("adult")
}*/


var amount:number=120;
if(amount>100){
    var tax=10
    var tax2:number=10/100*amount;
    var taxamount=tax2+amount;
    console.log("the tax on",taxamount,"is 10percent"," so the tax+amount");
}

var amount:number=510;
if(amount>500){
    var tax=25
    var tax2:number=25/100*amount;
    var taxamount=tax2+amount;
    console.log("the tax on",taxamount,"is 25percent"," so the tax+amount");
}
console .log(10/100*amount);
console .log(15/100*amount);
console .log(25/100*amount);

var nu


